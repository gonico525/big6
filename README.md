# プリズナートレーニング継続アプリ

BIG6 の実施を継続するための個人用 Web アプリ。起動した時点で「今日やること」が確定表示され、
実行の完了が最小操作で記録される。記録アプリではなく実行アプリとして作っている。

仕様書: [`docs/spec/prisoner-training-app-spec-v0.8.md`](docs/spec/prisoner-training-app-spec-v0.8.md)

## 使い方

### 初回セットアップ

1. アプリを開く（ローカルなら `npm run serve` → http://localhost:8000）
2. 設定 →「現在のステップ・級」で 6 種目の現在地を入力する
3. 必要なら 設定 →「ステップマスタ」で基準値・ステップ名を書籍の版に合わせて直す。
   説明欄には 60 ステップ分のフォームの要点が既定で入っている。書き換えても消しても構わない

ステップマスタは `standards.json` に入っている値が初回起動時に読み込まれる。
すでに起動したあとで `standards.json` を差し替えた場合は、
設定 → データ →「ステップマスタをテンプレートから読み込む」で反映する
（実施記録と設定は残る）。

### 日々の使い方

- ホームで種目カードをタップ → 種目実行画面
- スタート → 6 秒の秒読み → テンポカウント（毎秒ビープ、レップの先頭のみ低い音）
- リングの数字は「いま行っているレップの番号」。目標の回目を回しきると自動で止まる
- ステップ名の横の ⓘ をタップすると、そのステップの説明が出る
  （説明が入っているステップにのみ表示。カウント中でも開ける）
- 終了 → ±で実績を調整 → 記録する
- セット間は「休憩する」でカウントアップ
- 目標のセット数を終えると完了画面（記録する／もう1セット追加／休憩する）
- 途中で離脱しても、次回起動時に「続きから／ここまでを記録／破棄」を選べる

### ホーム画面に追加する（PWA）

Android / Chrome は 設定 →「アプリ」→「ホーム画面に追加」、
iOS / Safari は共有 →「ホーム画面に追加」。
追加するとブラウザの UI なしで起動し、圏外・機内モードでも動く
（一度開いた端末では、アプリ本体とステップマスタがキャッシュされる）。

新しい版が配信されると、次に開いたときホームに「新しい版があります」と出る。
「更新する」を押すと再読み込みされる。記録は端末に残るので消えない。

## 画面

| 画面 | 役割 |
|---|---|
| ホーム | 今日やること（当日目標・週の実績・昇格／降格の提案） |
| 種目実行 | テンポカウント・休憩・当日の記録 |
| 進捗 | 日毎の実施状況カレンダー、6種目×10ステップのグリッド、履歴の閲覧・追加・修正・削除 |
| 設定 | 進み方の係数、テンポと音、ステップマスタ、エクスポート／インポート |

## ファイル構成

```
index.html
app.js          … 画面と状態管理
logic.js        … 目標算出・昇格判定・降格判定・解禁判定（DOM に触れない純粋関数）
timer.js        … テンポカウント・休憩タイマー・音生成（Web Audio API）
store.js        … 保存の抽象化（localStorage への依存をここに閉じ込める）
pwa.js          … Service Worker の登録・更新検知・ホーム画面追加の導線
sw.js           … Service Worker（オフライン用のキャッシュ）
manifest.json   … ウェブアプリマニフェスト
icons/          … アプリアイコン（SVG が原本。PNG はそこから書き出したもの）
standards.json  … ステップマスタの初期テンプレート（名称・説明・基準値・unit・mode・perSide）
style.css
test/           … logic.js のテスト（node:test）
docs/spec/      … 仕様書
```

## 開発

```sh
npm test        # ロジックのテスト（依存パッケージなし）
npm run serve   # 静的サーバ。http://localhost:8000
```

ビルド工程はない。ES モジュールをそのまま配信する。

## デプロイ

`main` への push で GitHub Actions が Pages へデプロイする
（`.github/workflows/pages.yml`）。

初回のみ、リポジトリの **Settings → Pages → Build and deployment → Source** を
**GitHub Actions** に切り替える必要がある。

Wake Lock API・Web Audio API・Service Worker を使うため、HTTPS 配信が前提
（`localhost` は例外として動く）。

Service Worker は stale-while-revalidate で動く。キャッシュを返してから裏で取り直すため、
デプロイした内容はユーザーの**次回起動時**に反映される。
ビルド工程がなくファイル名にハッシュが付かないため、この方式にしている。
`sw.js` に**ファイルを追加したら `SHELL` にも足す**こと（足し忘れるとオフラインで欠ける）。
キャッシュを丸ごと捨てたいときだけ `sw.js` の `CACHE_VERSION` を上げる。

アイコンを直したときは `icons/*.svg` を編集し、PNG を書き出し直す
（Chrome などで SVG を開いて 192 / 512 / 180 px で保存すればよい）。

## データ

- 保存先は `localStorage`（キー: `prisoner-training-app`）
- 端末間の移行は 設定 → データ の JSON エクスポート／インポートで行う
- アカウントもサーバ同期もない

## 実装上の判断

仕様書に書かれていない箇所について、実装で以下のように解釈した。

- **級の初期値の扱い（仕様書 4.5）**: `initialState.level` を下限として使うのは、
  その種目が `initialState.step` に在籍している間だけとした。ステップが変われば動作自体が
  変わり、アプリ導入前の到達状況は引き継げないため。
- **漸進の基準値（仕様書 4.1(c)）**: 「+maxIncrement」の基準はレコードに保存された
  `targetOfDay`（実際に提示した目標）とした。基準値を後から修正しても、過去に提示した
  目標を計算し直さないため。
- **設定からの手動変更**: `type: "manual"` のステップ変更イベントとして記録し、
  昇格直後の係数（`initialRatio`）は適用しない。手動変更は「現在地の申告」であり、
  昇格による動作の変化とは性質が異なるため。

### ステップマスタの属性

`standards.json` の `unit` / `mode` / `perSide` は、基準値の表記から次のように決めた。
いずれも設定画面から変更できる。

- **`perSide: true`**: 基準が「各◯回」と表記されるステップ
  （プッシュアップ・スクワット・プルアップ・ハンドスタンドの Step7〜10）
- **`mode: "hold"` / `unit: "sec"`**: 基準が秒数のホールドであるステップ
  （ハンドスタンド Step1〜3）
- **`mode: "free"`**: レップを 4 フェーズに分解できない動作
  （ブリッジ Step7・8 のウォール・ウォーキング）

## ライセンス

コード（`app.js` / `logic.js` / `timer.js` / `store.js` / `pwa.js` / `sw.js` など）は
[MIT License](LICENSE) の下で公開する。

`standards.json` のステップ名・説明文は、プリズナートレーニング（Convict Conditioning,
Paul Wade 著）のフォーム要点を自分の言葉で要約したもの。トレーニング内容・メソッド自体の
権利は原著に帰属する。
